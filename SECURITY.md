# Security policy

Use the repository's **Security** tab to submit a private vulnerability
report. If private reporting is unavailable, open a minimal issue requesting a
private contact channel and do not include exploit details.

`VulnPrism` is decision support. Its local OSV data may be incomplete or
stale, and its dependency-free version comparison is intentionally limited.
A clean report is not proof that a dependency is vulnerability-free.
